#include "pch.h"
#include "..\Clover\Server.h"
#include <memory>
using ::testing::AtLeast;

TEST(Start, SocketIsBind) {
	/*std::shared_ptr<MockITcpSocket> socket(new MockITcpSocket());
	EXPECT_CALL(*socket, Bind()).Times(1);

	std::unique_ptr<IServer> s(new Server(socket));
	s->Start();*/
}